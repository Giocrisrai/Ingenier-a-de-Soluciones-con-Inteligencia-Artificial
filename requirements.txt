# Ingeniería de Soluciones con Inteligencia Artificial
# Dependencias del curso (alineadas con pyproject.toml).
# Instalación recomendada del curso: uv + uv.lock → `uv sync` (ver README).
# pip install -r requirements.txt es respaldo si no puedes usar uv.

# --- LLMs y APIs (Groq) ---
groq>=0.11.0
langchain-groq>=0.2.0
langchain>=1.0.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langchain-classic>=0.3.0
langsmith>=0.3.0
langgraph>=0.3.0

# --- Agentes ---
# El extra [litellm] es obligatorio: CrewAI llega a Groq via LiteLLM (prefijo "groq/").
crewai[litellm]>=0.100.0
crewai-tools>=0.38.0

# --- RAG y Embeddings ---
# Groq no ofrece endpoint de embeddings: se usa un modelo local de HuggingFace.
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0
faiss-cpu>=1.7.0
tiktoken>=0.5.0

# --- Data y Analisis ---
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0

# --- Visualizacion ---
matplotlib>=3.7.0
plotly>=5.15.0
seaborn>=0.12.0

# --- Web y Apps ---
streamlit>=1.30.0
requests>=2.31.0
httpx>=0.25.0

# --- API del artefacto desplegable (RA3 / IL3.5, carpeta deploy/) ---
fastapi>=0.136.3
uvicorn[standard]>=0.42.0
slowapi>=0.1.9

# --- Utilidades ---
python-dotenv>=1.0.0
pydantic>=2.0.0
wikipedia>=1.4.0
lxml>=6.0.2

# --- Jupyter ---
jupyter>=1.0.0
ipykernel>=6.0.0

# --- Testing ---
pytest>=7.0.0
